node_modules
dist
coverage
.astro

# The built site is committed: github pages serves the repo's /docs folder.
!/docs

# Staging directory for surge.sh docs previews, built in CI.
surge

# Built site. Deployed by the Pages workflow from a fresh build, never committed.
docs
